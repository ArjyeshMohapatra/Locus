
name: CI

on:
  push:
    branches: [ main, 'ci/**' ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.13]
    steps:
      - uses: actions/checkout@v4

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            ui/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('ui/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi
          pip install pytest ruff

      - name: Lint (ruff)
        run: |
          ruff check .
        working-directory: backend

      - name: Check formatting (black)
        run: |
          pip install black
          black --check .
        working-directory: backend

      - name: Type check (mypy)
        run: |
          pip install mypy
          mypy .
        working-directory: backend

      - name: Security checks (bandit + pip-audit)
        run: |
          pip install bandit pip-audit
          bandit -r .
          pip-audit --quiet
        working-directory: backend

      - name: Run tests with coverage (uses test SQLite DB)
        env:
          DATABASE_URL: sqlite:///./backend/test.db
        run: |
          pip install coverage
          coverage run -m pytest -q
          coverage xml -o coverage.xml
          # remove test DB after tests
          if [ -f backend/test.db ]; then rm backend/test.db; fi
        working-directory: backend

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: backend/coverage.xml

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install and run UI tests and build
        run: |
          cd ui
          npm ci
          npm test
          npm run build

