[package]
name = "locus_tauri"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.5", features = [ "dialog-message", "dialog-ask", "dialog-confirm", "shell-execute", "shell-open", "dialog-open"] }
